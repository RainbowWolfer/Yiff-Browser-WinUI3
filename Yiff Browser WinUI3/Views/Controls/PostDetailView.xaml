<UserControl
    x:Class="Yiff_Browser_WinUI3.Views.Controls.PostDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Yiff_Browser_WinUI3.Views.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

	<Grid>
		<Grid.DataContext>
			<local:PostDetailViewModel x:Name="ViewModel" E621Post="{x:Bind E621Post,Mode=OneWay}"/>
		</Grid.DataContext>

		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="auto"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<Button Content="-----" Click="Button_Click"/>

		<Grid x:Name="ContentRootGrid" Grid.Row="1" Grid.Column="1">
			<Image x:Name="PreviewImage" Source="{Binding E621Post.Preview.URL}"/>
			<!--<Image x:Name="SampleImage" Source="{Binding E621Post.Sample.URL}"/>-->
			<!--<Image x:Name="MainImage" Source="{Binding E621Post.File.URL}"/>-->
		</Grid>

	</Grid>
</UserControl>
